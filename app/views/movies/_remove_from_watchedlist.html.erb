<%= form_for(current_user.watchedlist_items.find_by(movie_id: movie.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Remove from Watchedlist", class: "btn btn-danger" %>
<% end %>
