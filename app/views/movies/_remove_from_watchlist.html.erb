<%= form_for(current_user.watchlist_items.find_by(movie_id: movie.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Remove from Watchlist", class: "btn" %>
<% end %>
